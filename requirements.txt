# COMAC-HQY-专利助手 运行依赖
# 基础库 (必装)
pyyaml>=6.0

# Web 服务 (app.py / serve 子命令)
# 仅用标准库 http.server,无第三方依赖

# Phase A:prior art adapter 抓取层
requests>=2.31
beautifulsoup4>=4.12
lxml>=4.9
playwright>=1.40           # Google Patents 反爬;`python -m playwright install chromium`

# ---- 开发依赖 ----
pytest>=7.0